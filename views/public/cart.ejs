<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title }) %>
  <body>
    <%- include('../partials/header') %>

    <main class="page-shell">
      <div class="container">
        <%- include('../partials/page-header', {
          eyebrow: 'Guest-First Buyer Flow',
          title: 'Shopping Cart',
          subtitle: 'Review selected items before guest checkout or optional login.',
          actions: [{ label: 'Continue Browsing', href: '/', variant: 'secondary' }]
        }) %>

        <section class="card cozy">
          <div class="empty-state">
            <h3>Cart interaction will be active in Stage 4</h3>
            <p>
              Quantity control, variants, notes, and cart totals will be connected in buyer
              module implementation with guest-first checkout.
            </p>
            <div class="actions">
              <a class="btn btn-primary" href="/checkout">Go to Checkout</a>
            </div>
          </div>
        </section>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
