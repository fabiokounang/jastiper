<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title }) %>
  <body>
    <%- include('../partials/header') %>
    <main class="page-shell">
      <div class="container">
        <%- include('../partials/page-header', {
          title: 'Checkout',
          subtitle: 'Complete shipping details and payment method.',
          actions: [{ label: 'Back to Cart', href: '/cart', variant: 'secondary' }]
        }) %>

        <section class="grid two-col">
          <article class="card">
            <h3>Checkout Form (Stage 4)</h3>
            <p>Receiver info, shipping address, notes, and payment method.</p>
          </article>
          <article class="card subtle">
            <h3>Payment Preparation (Stage 5)</h3>
            <p>QRIS and MNC callback architecture will be connected here.</p>
          </article>
        </section>
      </div>
    </main>
    <%- include('../partials/footer') %>
  </body>
</html>
