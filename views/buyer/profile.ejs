<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title }) %>
  <body>
    <%- include('../partials/header') %>
    <main class="page-shell">
      <div class="container">
        <%- include('../partials/page-header', {
          title: 'Buyer Profile',
          subtitle: 'Manage personal data and shipping preference.',
          actions: [{ label: 'Back to Dashboard', href: '/buyer/dashboard', variant: 'secondary' }]
        }) %>

        <section class="grid two-col">
          <article class="card">
            <h3>Account Details</h3>
            <p>Full name, phone, city, and profile updates.</p>
          </article>
          <article class="card subtle">
            <h3>Shipping Addresses</h3>
            <p>Multiple addresses with default selection for faster checkout.</p>
          </article>
        </section>
      </div>
    </main>
    <%- include('../partials/footer') %>
  </body>
</html>
