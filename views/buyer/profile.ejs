<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title }) %>
  <body>
    <%- include('../partials/header') %>

    <main class="page-shell">
      <div class="container">
        <%- include('../partials/page-header', {
          eyebrow: 'Buyer',
          title: 'Profile',
          subtitle: 'Update personal information and delivery preferences.',
          actions: [{ label: 'Back to Dashboard', href: '/buyer/dashboard', variant: 'secondary' }]
        }) %>

        <section class="grid two-col">
          <article class="card soft stack">
            <h3>Personal data</h3>
            <p class="muted">Name, phone, and regional contact details for checkout convenience.</p>
          </article>
          <article class="card stack">
            <h3>Address list</h3>
            <p class="muted">Manage multiple shipping addresses and default destination options.</p>
          </article>
        </section>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
