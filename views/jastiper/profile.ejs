<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title }) %>
  <body>
    <%- include('../partials/header') %>

    <main class="page-shell">
      <div class="container">
        <%- include('../partials/page-header', {
          title: 'Jastiper Profile',
          subtitle: 'Keep your shop identity and payout profile complete for buyer trust.',
          actions: [{ label: 'Back to Dashboard', href: '/jastiper/dashboard', variant: 'secondary' }]
        }) %>

        <section class="grid two-col">
          <article class="card cozy">
            <h3>Shop Identity</h3>
            <p>Shop name, profile photo, bio, and Instagram account for social credibility.</p>
          </article>
          <article class="card subtle">
            <h3>Bank Information</h3>
            <p>Optional payout information for easier transaction settlement flow.</p>
          </article>
        </section>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
