<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title }) %>
  <body>
    <%- include('../partials/header') %>

    <main class="page-shell">
      <div class="container">
        <%- include('../partials/page-header', {
          title: 'User Monitoring',
          subtitle: 'Search users, filter status, and prepare moderation actions clearly.',
          actions: [{ label: 'Back to Dashboard', href: '/admin/dashboard', variant: 'secondary' }]
        }) %>

        <section class="card cozy">
          <div class="toolbar">
            <input type="text" placeholder="Search by name, email, phone" />
            <select>
              <option>All Roles</option>
              <option>Admin</option>
              <option>Jastiper</option>
              <option>Buyer</option>
            </select>
            <select>
              <option>All Status</option>
              <option>Active</option>
              <option>Suspended</option>
              <option>Pending</option>
            </select>
          </div>

          <div class="empty-state">
            <h3>User table will be active in Stage 6</h3>
            <p>
              This page will include verification approval/rejection, account suspend/activate,
              and detailed user drill-down.
            </p>
          </div>
        </section>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
